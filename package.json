{
  "name": "dragonslaircms",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute dragonslaircms --file=schema.sql",
    "db:schema:remote": "wrangler d1 execute dragonslaircms --file=schema.sql --remote"
  },
  "dependencies": {
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.106.0"
  }
}
