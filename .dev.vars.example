# Copy to .dev.vars for local development (wrangler dev reads this file).
# Generate values with: openssl rand -hex 32
ADMIN_SESSION_SECRET=replace-with-random-32-plus-chars
READER_SESSION_SECRET=replace-with-random-32-plus-chars
